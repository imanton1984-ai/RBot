[package]
name = "data_types"
version = "0.1.0"
edition = "2021"

[lib]
name = "data_types"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1.0"
