[package]
name = "svc_writer"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "svc_writer"
path = "src/main.rs"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
tracing = "0.1"
data_writer = { path = "../../data_writer" }
database = { path = "../../database" }

